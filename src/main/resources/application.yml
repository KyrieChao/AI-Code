spring:
  application:
    name: chao-ai-code
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ai_code
    username: root
    password: 893760
  profiles:
    active: local
server:
  port: 9000
  servlet:
    context-path: /api
springdoc:
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.chao.aicode.controller

knife4j:
  enable: true
  setting:
    language: zh_cn
